export class DriveFolder {
    public name:string;
    public drive_id:string;

    public constructor(name:string, drive_id:string) {
        this.name = name;
        this.drive_id = drive_id;
    }
}